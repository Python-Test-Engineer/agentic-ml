# Task

To create agentic ML learning loop.

Given a dataset with specific metrics that can be evaluated, I want to run an eval loop that improves the metric.

Create a PLAN.md that will outline this without creating code.

It is based on https://github.com/karpathy/autoresearch - ensure you explain deeply and clearly what the technical Python spec will need to flush out. Based on this file, we will create a SPEC.md to specify the code implementation.

Also create a small 100 row dataset of 2 variables, one result so that it can be used as data for this project. If you need to change the structure of the data set do so.