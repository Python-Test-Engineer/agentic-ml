# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Intent

This is a new project to build agentic ML systems. The repository is in its initial stage — no code, dependencies, or structure exist yet.

## Setup

Update this file with build, lint, and test commands once the project stack is established (e.g., `pip install -e .[dev]`, `pytest`, `ruff check .`).
